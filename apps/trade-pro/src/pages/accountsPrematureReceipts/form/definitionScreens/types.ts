export type TBankName = {
  BankName: string;
};
export type TBankNameHistory = {
  Id: number;
  BankName: string;
};
export type TDefineOtherParties = {
  PartyName: string;
  IsActive: boolean; // when isActive Checked Then true else false
};
export type TDefineOtherPartiesHistory = {
  PartyName: string;
  IsActive: boolean; // when isActive Checked Then true else false
};
